#!/bin/sh

# Stable fnm path (no multishell symlinks) — `see .cursor/FNM-MULTISHELLS-FIX.md`
# export PATH="$HOME/Library/Application Support/fnm/aliases/default/bin:$PATH"

# Type check
echo "🔍 Running type check..."
corepack yarn type-check || exit 1

# Lint check
echo "🧹 Running lint check..."
corepack yarn lint:ci || exit 1

# Format check
echo "💅 Running format check..."
corepack yarn format:check || exit 1

# Run tests
echo "🧪 Running tests..."
corepack yarn test || exit 1

echo "✅ All pre-push checks passed!"
